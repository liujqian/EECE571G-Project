export interface GameResponse {
    id: number,
    card_price: string;
    has_completed: boolean;
    host_address: string;
    host_fee: string;
    is_valid: boolean;
    last_draw_time: string;
    pool_value: string;
    start_time: string;
    turn_time: string;
}
